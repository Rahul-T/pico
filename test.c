#include <stdio.h>

// This is a very long line to check what happens when it is ALMOST too long

// This is a very long line to make sure that our ability to wrap lines around  // still works correctly

int main() {
    printf("Hello World");
    for(int i = 0; i < 3 i++){
        //Wow look at all this sytax highlighting
        continue;
    }
    return 0;
}

struct {
    char character;
    int number;
    long longnumber;
};

// Just another method to fill up space
int anothermethod() {
    const i = 0;
    while(i < 0) {
        break;
    }
    return 0;
}




//Look at how well our scrolling works!